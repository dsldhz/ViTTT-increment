# ViT³ / DiTTT 代码导读

> 本文根据当前仓库代码整理，面向第一次阅读项目的开发者。分析基于当前提交 `e347758`。文中的“默认值”来自代码或配置文件；“建议”与“注意事项”是根据实现给出的复现提示。

## 1. 项目解决什么问题

本仓库是论文 **ViT³: Unlocking Test-Time Training in Vision** 的官方 PyTorch 实现。核心思想是把普通注意力中的序列交互改写为一次“内循环学习”：

1. 从输入 token 生成 `Q/K/V`；
2. 用当前样本的 `K/V` 临时更新一个很小的内部模型；
3. 用更新后的内部模型处理 `Q`；
4. 内部更新发生在每次前向传播中，不会像普通优化器那样永久改写模型参数。

因此，这里的 **Test-Time Training（TTT）不是在测试集上执行若干次外部 optimizer step**，而是模型层本身在 `forward()` 内，根据当前样本完成一次可微、按样本独立的临时参数更新。

仓库覆盖四类任务：

| 目录 | 任务 | 主模型/框架 |
|---|---|---|
| `vittt/` | ImageNet 图像分类 | ViT³、H-ViT³，原生 PyTorch DDP |
| `segmentation/` | ADE20K 语义分割 | H-ViT³ + UperNet，基于 MMSegmentation |
| `detection/` | COCO 检测与实例分割 | H-ViT³ + Mask R-CNN，基于 MMDetection |
| `dittt/` | ImageNet 类条件图像生成 | DiT³，基于 DiT 与潜空间扩散 |

## 2. 仓库总览

```text
.
├── readme.md                       # 项目总说明、论文、最小示例和各任务入口
├── ttt_block.py                    # 可独立复用的最小 TTT 模块
├── figures/
│   └── fig1_ttt.png                # README 中的论文示意图
├── vittt/                          # 分类训练、模型、配置和数据读取
├── segmentation/                   # 合并到 MMSegmentation 的增量文件
├── detection/                      # 合并到 MMDetection 的增量文件
├── dittt/                          # DiT³ 训练、采样和扩散过程
├── LICENSE                         # 仓库根目录 MIT License
└── .gitignore
```

最重要的代码关系如下：

```text
ttt_block.py
     │
     ├── vittt/models/ttt_block.py（内容完全相同）
     └── dittt/ttt_block.py        （内容完全相同）

TTT
 ├── ViT³ 单尺度分类模型
 ├── H-ViT³ 四阶段分类模型
 └── DiT³ 扩散模型中的 token mixing

H-ViT³ 下游版骨干
 ├── segmentation/mmseg/models/backbones/vittt.py
 └── detection/mmdet/models/backbones/vittt.py
```

三个 `ttt_block.py` 当前 SHA-256 一致。根目录版本适合阅读和复制；分类与生成目录中的副本用于各自脚本按当前工作目录导入。

## 3. 环境配置

### 3.1 分类任务 `vittt/`

`vittt/README.md` 明确给出的版本是：

- Python 3.9
- PyTorch 1.11.0
- torchvision 0.12.0
- timm 0.4.12
- numpy、einops、yacs

从实际导入还可以看到：

- `PyYAML`：读取 YAML 配置；
- `termcolor`：彩色日志；
- `Pillow`：图像读取，通常由 torchvision 间接安装；
- `fvcore`：只被开发用的 `temp.py` 用于 FLOPs 统计，不是训练必需项。

仓库没有为分类部分提供 `requirements.txt` 或 Conda 文件。可按下面方式创建环境，PyTorch/CUDA 安装命令应按机器驱动选择官方对应构建：

```bash
conda create -n vittt python=3.9
conda activate vittt

# 安装与本机 CUDA 匹配的 PyTorch 1.11.0 / torchvision 0.12.0 后：
pip install timm==0.4.12 numpy einops yacs PyYAML termcolor Pillow

# 仅运行 vittt/temp.py 时需要
pip install fvcore
```

不建议直接把 `timm` 升到最新版：代码使用了 `timm.models.registry`、`timm.models.layers` 等旧版 API。

### 3.2 图像生成任务 `dittt/`

生成分支提供了 `dittt/environment.yml`：

```yaml
name: DiTTT
channels:
  - pytorch
  - nvidia
dependencies:
  - python >= 3.8
  - pytorch >= 1.13
  - torchvision
  - pytorch-cuda=11.7
  - pip:
    - timm
    - diffusers
    - accelerate
    - tqdm
```

创建方式：

```bash
cd dittt
conda env create -f environment.yml
conda activate DiTTT
```

训练和采样会通过 `diffusers.AutoencoderKL.from_pretrained()` 获取
`stabilityai/sd-vae-ft-mse` 或 `stabilityai/sd-vae-ft-ema`，首次运行需要可访问模型源或提前准备好本地缓存。

`environment.yml` 对 Python、PyTorch 和 pip 包只给了下限或未固定版本。若要长期复现实验，建议在环境验证成功后额外导出精确 lock/spec 文件。

### 3.3 检测与分割

这两个目录不是完整的 OpenMMLab 工程，而是需要覆盖/合并到官方仓库中的“增量文件”：

- 分割：MMSegmentation **1.2.2**；
- 检测：MMDetection **3.3.0**。

正确流程是：

1. 分别克隆对应版本的完整官方仓库；
2. 按其官方文档安装匹配的 PyTorch、MMCV、MMEngine 等依赖；
3. 将本仓库 `segmentation/` 或 `detection/` 中的文件按相同相对路径合并进去；
4. 另外确保环境中安装了 `timm`；
5. 修改数据路径并放置 ImageNet 预训练骨干权重。

本仓库缺少配置继承所需的 `models/`、`schedules/`、`default_runtime.py`，也缺少 `mmdet`/`mmseg` 的绝大多数官方模块，因此不能直接在当前子目录中独立训练。

## 4. 核心 TTT 模块

实现位于：

- `ttt_block.py`
- `vittt/models/ttt_block.py`
- `dittt/ttt_block.py`

### 4.1 输入、参数和两条分支

输入为 `x: [B, N, C]`，并显式传入特征图高宽 `h, w`。设头数为 `Hn`，每头维度为 `d = C / Hn`。

`qkv` 投影输出 `3C + 3d` 个通道，并拆成两组：

| 分支 | 张量 | 形状/用途 |
|---|---|---|
| 简化 SwiGLU | `q1, k1, v1` | 每个都是总宽度 `C`，重排成 `[B, Hn, N, d]` |
| 3×3 深度卷积 | `q2, k2, v2` | 每个宽度 `d`，重排成 `[B, d, h, w]` |

可学习的内部初始参数是：

- `w1, w2: [1, Hn, d, d]`，用于简化 SwiGLU；
- `w3: [d, 1, 3, 3]`，用于 depth-wise convolution；
- 最后的 `proj: Linear(C + d, C)` 融合两条分支。

### 4.2 一次前向中的“内训练”

简化 SwiGLU 分支先用 `K` 计算：

```text
z1 = K @ w1
z2 = K @ w2
a  = z2 * sigmoid(z2)
```

代码没有显式构造标量 inner loss，也没有调用一次嵌套的 `backward()`。它直接使用手工推导的闭式梯度，使内部模型输出与 `V` 对齐，然后执行一步：

```text
w1' = w1 - lr * g1
w2' = w2 - lr * g2
```

梯度在更新前按 `g / (norm(g) + 1)` 缩放，以增强稳定性。更新后的权重再作用于 `Q`：

```text
x1 = (Q @ w1') * SiLU(Q @ w2')
```

卷积分支同样从 `K/V` 推导每个样本、每个通道的 3×3 卷积核梯度，得到临时权重 `w3'`，再对 `Q` 做分组卷积：

```text
x2 = DWConv3x3(Q, w3')
```

最后拼接 `x1` 与 `x2` 并线性投影回 `C` 维。

### 4.3 容易误解的几点

- 临时权重是局部变量，没有赋回 `self.w1/self.w2/self.w3`，所以不同 batch 不会累积测试样本信息。
- 更新后的权重包含 batch 维，每个样本都有自己的内部模型。
- 外层训练仍然可以通过整个计算图更新初始权重和投影层。
- 序列交互没有构造 `N×N` 注意力矩阵；在通道规模固定时，计算和显存对 token 数 `N` 近似线性增长。
- 可选的 RoPE 只施加到第一分支的 `Q/K`；第二条卷积分支天然利用二维局部结构。

## 5. 分类分支 `vittt/`

### 5.1 代码结构

| 文件/目录 | 作用 |
|---|---|
| `README.md` | 分类环境、ImageNet 数据结构、预训练结果、训练/评估命令 |
| `main.py` | 普通分类 DDP 训练、验证、吞吐率测试 |
| `main_ema.py` | 在 `main.py` 上增加 EMA 教师与 MESA 自蒸馏 |
| `config.py` | YACS 默认配置、YAML 递归合并、命令行覆盖 |
| `cfgs/*.yaml` | 九个模型变体的最小差异配置 |
| `models/build.py` | 按 `MODEL.TYPE` 构造 ViT³ 或 H-ViT³ |
| `models/vittt.py` | 单尺度 ViT³ |
| `models/h_vittt.py` | 四阶段层次化 H-ViT³ |
| `models/ttt_block.py` | 核心 TTT 层 |
| `data/build.py` | ImageNet 数据集、增强、DataLoader、Mixup/CutMix |
| `data/cached_image_folder.py` | 普通目录/ZIP 数据集与内存缓存 |
| `data/zipreader.py` | `xxx.zip@/path` 风格的 ZIP 随机读取 |
| `data/samplers.py` | 分布式验证和分片缓存使用的子集随机采样器 |
| `optimizer.py` | SGD/AdamW 与 weight-decay 参数分组 |
| `lr_scheduler.py` | cosine、linear、step 三种按 iteration 更新的学习率 |
| `utils.py` | checkpoint、预训练权重适配、梯度范数、分布式 reduce |
| `utils_ema.py` | 支持 EMA 状态的 checkpoint 工具 |
| `logger.py` | rank 0 控制台和文件日志 |
| `temp.py` | TTT 冒烟测试、前后向、参数量/FLOPs/速度实验脚本 |

`models/__init__.py` 和 `data/__init__.py` 只负责分别导出 `build_model`、`build_loader`。

### 5.2 单尺度 ViT³

`models/vittt.py` 从 timm 的 Vision Transformer 实现改造而来，真正使用的主路径是：

```text
224×224 RGB
  → PatchEmbed(kernel=stride=16)
  → 14×14 = 196 tokens
  → 12 × [CPE → LN → TTT → LN → MLP]
  → LayerNorm
  → token 平均池化
  → 1000 类线性头
```

每个 Block 首先使用 3×3 depth-wise convolution 作为条件位置编码 CPE，然后是 TTT 残差分支和 MLP 残差分支。它没有在正常前向中使用 class token，最终直接对所有 patch token 求均值。

| 模型 | Patch | 宽度 `C` | 深度 | 头数 | 配置 DropPath | README 参数量 |
|---|---:|---:|---:|---:|---:|---:|
| ViT³-T | 16 | 192 | 12 | 6 | 0.0 | 6M |
| ViT³-S | 16 | 384 | 12 | 6 | 0.1 | 24M |
| ViT³-B | 16 | 768 | 12 | 12 | 0.4 | 90M |

文件中保留了较多从 timm ViT 继承而来的预训练加载和注册代码，其中一部分（例如 class/distillation token 相关方法）不在本项目正常构建与前向路径中使用。

### 5.3 层次化 H-ViT³

`models/h_vittt.py` 面向分类构建四阶段骨干：

```text
224×224
  → 卷积 Stem，降采样 4 倍：56×56
  → Stage 1
  → PatchMerging：28×28
  → Stage 2
  → PatchMerging：14×14
  → Stage 3
  → PatchMerging：7×7
  → Stage 4
  → BatchNorm1d + 全局平均池化 + 分类头
```

Stem 由多层 3×3/1×1 卷积组成；PatchMerging 使用
`1×1 扩张 → 3×3 depth-wise stride=2 → 1×1 投影`。每个 TTT Block 包含：

```text
CPE
→ LayerNorm + 带二维 RoPE 的 TTT + residual
→ LayerNorm + 带 3×3 depth-wise conv 的 MLP + residual
```

| 模型 | 四阶段维度 | 四阶段深度 | 四阶段头数 | DropPath | MESA 系数 |
|---|---|---|---|---:|---:|
| H-ViT³-T | `[64,128,320,512]` | `[1,3,9,4]` | `[2,4,10,16]` | 0.2 | 1.0 |
| H-ViT³-S | `[64,128,320,512]` | `[2,6,18,8]` | `[2,4,10,16]` | 0.4 | 1.5 |
| H-ViT³-B | `[96,192,448,640]` | `[2,6,18,8]` | `[3,6,14,20]` | 0.6 | 2.0 |

每个 H-ViT³ 尺寸同时有普通 YAML 和 `_mesa.yaml`。MESA 配置应配合 `main_ema.py` 使用。

### 5.4 MESA 与 EMA

`main_ema.py` 的额外逻辑是：

1. 用 `timm.ModelEma` 跟踪模型指数滑动平均，基础 decay 为 `0.9996`，并按全局 batch size 调整；
2. 训练前 25% epoch 只使用原监督损失；
3. 从 25% 训练进度开始，用 EMA 模型的 softmax 输出作为软目标；
4. 总损失为 `supervised_loss + MESA × teacher_loss`；
5. 分别记录普通模型和 EMA 模型的最佳精度与 checkpoint。

`--model-ema` 被定义成 `store_true` 且默认值已经为 `True`，因此当前 CLI 没有直接关闭 EMA 的反向开关。

### 5.5 默认训练配置

`config.py` 的核心默认值：

| 项目 | 默认值 |
|---|---|
| 单卡 batch size | 128 |
| 输入大小 | 224 |
| worker 数 | 8 |
| epoch | 300 |
| warmup | 20 epoch |
| 优化器 | AdamW |
| 基础学习率 | `5e-4` |
| weight decay | `0.05` |
| 学习率策略 | cosine |
| 最低学习率 | `5e-6` |
| 梯度裁剪 | 5.0 |
| label smoothing | 0.1 |
| Mixup / CutMix | 0.8 / 1.0 |
| AMP | 默认关闭，命令行 `--amp` 开启 |
| 自动续训 | 开启 |

实际学习率会乘以：

```text
单卡 batch size × world size / 512
```

训练增强由 timm `create_transform()` 创建，包含 RandAugment、color jitter 和 random erasing；验证使用 resize、center crop 与 ImageNet mean/std。损失按 Mixup/CutMix、label smoothing、普通交叉熵的优先级选择。

### 5.6 ImageNet 数据与运行入口

普通目录模式：

```text
/path/to/imagenet/
├── train/
│   ├── n01440764/
│   └── ...
└── val/
    ├── n01440764/
    └── ...
```

ZIP 模式要求 `train.zip`、`val.zip` 及对应的 `train_map.txt`、`val_map.txt`。`cache_mode=full` 缓存全部字节，`part` 只缓存当前 rank 的数据分片。

脚本使用相对导入风格，应从 `vittt/` 目录运行；并且即使只评估，也会初始化 NCCL 分布式环境，因此需要由分布式 launcher 设置 `RANK/WORLD_SIZE/LOCAL_RANK`：

```bash
cd vittt

# 普通 ViT³ 训练
python -m torch.distributed.launch --nproc_per_node=8 \
  main.py --cfg cfgs/vittt_s.yaml \
  --data-path /path/to/imagenet --output /path/to/output --amp

# H-ViT³ + EMA/MESA 训练
python -m torch.distributed.launch --nproc_per_node=8 \
  main_ema.py --cfg cfgs/h_vittt_s_mesa.yaml \
  --data-path /path/to/imagenet --output /path/to/output --amp

# 评估
python -m torch.distributed.launch --nproc_per_node=8 \
  main_ema.py --cfg cfgs/h_vittt_s_mesa.yaml \
  --data-path /path/to/imagenet --eval --resume /path/to/model.pth
```

输出目录最终会变成 `<output>/<MODEL.NAME>/<TAG>`，包含日志、配置快照和 checkpoint。

## 6. 分割分支 `segmentation/`

### 6.1 结构和数据流

| 文件 | 作用 |
|---|---|
| `README.md` | MMSegmentation 版本、权重、训练与测试命令 |
| `configs/_base_/datasets/ade20k.py` | ADE20K 数据路径、增强、DataLoader、mIoU |
| `configs/vittt/vittt_{tiny,small,base}.py` | UperNet + H-ViT³ 三种骨干配置 |
| `mmseg/models/backbones/vittt.py` | 支持动态尺寸的四阶段 H-ViT³ |
| `mmseg/models/backbones/__init__.py` | 将三个骨干工厂导出到 MMSegmentation |

完整网络来自配置继承：

```text
输入图像
  → H-ViT³ 四阶段特征 `[C1,C2,C3,C4]`
  → UperNet decode head
  → 150 类 ADE20K 分割图
           └→ Stage 3 特征还进入 auxiliary head
```

下游骨干与分类版的主要差别：

- 支持非固定、非正方形输入，Stem 和 PatchMerging 会对不能整除的边界补零；
- TTT 每头维度固定为 32，因此头数为 `dim // 32`；
- RoPE 按当前实际 `H/W` 动态生成；
- 不再做全局池化和分类，而是输出四个 `[B,C,H,W]` 特征图；
- 用 `MMSEG_MODELS` registry 注册，并通过 `init_cfg` 加载 ImageNet checkpoint。

### 6.2 ADE20K 配置

- 修改 `configs/_base_/datasets/ade20k.py` 中的 `data_root`；
- 训练 crop 为 `512×512`；
- 随机缩放比例 `0.5～2.0`、随机裁剪、水平翻转、光度增强；
- 验证指标为 mIoU；
- 三个配置都训练 160K iterations，默认 8 GPU、每卡 2 张；
- 优化器为 AdamW，`lr=6e-5`、`weight_decay=0.01`；
- 前 1500 iterations 线性 warmup，之后 PolyLR；
- Tiny 配置使用 `AmpOptimWrapper`，Small/Base 当前配置写的是普通 `OptimWrapper`。

通道配置：

| 变体 | Backbone/UperNet 输入通道 | 辅助头输入 | DropPath |
|---|---|---:|---:|
| Tiny | `[64,128,320,512]` | 320 | 0.2 |
| Small | `[64,128,320,512]` | 320 | 0.4 |
| Base | `[96,192,448,640]` | 448 | 0.5 |

在合并后的 MMSegmentation 根目录运行：

```bash
torchrun --nproc_per_node=<GPU_NUM> \
  tools/train.py configs/vittt/vittt_small.py --launcher=pytorch

python tools/test.py <CONFIG_FILE> <CHECKPOINT_FILE>
```

## 7. 检测分支 `detection/`

### 7.1 结构和数据流

| 文件 | 作用 |
|---|---|
| `README.md` | MMDetection 版本、COCO 结果、权重和命令 |
| `configs/_base_/datasets/coco_detection.py` | 只含 bbox 的 COCO 数据配置 |
| `configs/_base_/datasets/coco_instance.py` | bbox + mask 的 COCO 实例分割配置 |
| `configs/vittt/*_mrcnn_{1x,3x}.py` | Tiny/Small/Base × 12/36 epoch 配置 |
| `mmdet/models/backbones/vittt.py` | 动态尺寸 H-ViT³ 检测骨干 |
| `mmdet/models/backbones/__init__.py` | 导出三个骨干工厂 |
| `mmdet/utils/logger.py` | 本增量代码使用的 root logger 与图像尺度日志 |
| `mmdet/utils/__init__.py` | 合并到完整 MMDetection 后的工具导出 |

主数据流：

```text
COCO 图像
  → H-ViT³ 四阶段特征
  → FPN
  → Mask R-CNN
  ├── bbox 分类/回归
  └── instance mask
```

检测与分割中的 `vittt.py` 除 registry 和 logger 导入外基本相同。

### 7.2 COCO 与训练配置

修改 `configs/_base_/datasets/coco_instance.py` 中的 `data_root`，期望至少包含：

```text
coco/
├── train2017/
├── val2017/
└── annotations/
    ├── instances_train2017.json
    └── instances_val2017.json
```

共同设置：

- Mask R-CNN + FPN；
- ImageNet 预训练 H-ViT³；
- gradient checkpointing 开启；
- AdamW，`lr=2e-4`、`weight_decay=0.05`；
- AMP OptimWrapper；
- 多尺度 RandomChoiceResize，可选中间随机裁剪；
- 评价 bbox mAP 和 mask mAP。

| 变体 | FPN 输入通道 | 1x DropPath | 3x DropPath |
|---|---|---:|---:|
| Tiny | `[64,128,320,512]` | 0.1 | 0.3 |
| Small | `[64,128,320,512]` | 0.1 | 0.4 |
| Base | `[96,192,448,640]` | 0.1 | 0.6 |

- 1x：12 epoch，学习率在 epoch 8、11 衰减；
- 3x：36 epoch，学习率在 epoch 27、33 衰减。

在合并后的 MMDetection 根目录运行：

```bash
torchrun --nproc_per_node=<GPU_NUM> \
  tools/train.py configs/vittt/vittt_s_mrcnn_3x.py --launcher=pytorch

python tools/test.py <CONFIG_FILE> <CHECKPOINT_FILE>
```

## 8. 生成分支 `dittt/`

### 8.1 代码结构

| 文件/目录 | 作用 |
|---|---|
| `README.md` | 环境、结果、训练、采样和 FID 评估说明 |
| `environment.yml` | DiTTT Conda 环境 |
| `models.py` | DiT³ 模型、条件嵌入、adaLN-Zero、模型规格 |
| `train.py` | ImageNet DDP 训练、VAE 编码、EMA、checkpoint |
| `sample.py` | 单进程/单卡类条件采样，输出 `sample.png` |
| `sample_ddp.py` | 多卡生成 50K 图片并打包 `.npz` |
| `download.py` | 加载自定义 checkpoint，兼容原 DiT 权重下载逻辑 |
| `ttt_block.py` | 与根目录一致的 TTT 实现 |
| `diffusion/__init__.py` | 构造扩散过程，默认 1000 步线性噪声 |
| `diffusion/gaussian_diffusion.py` | 前向加噪、训练损失、DDPM/DDIM 采样 |
| `diffusion/respace.py` | 从 1000 步中选择子时间步以加速采样 |
| `diffusion/timestep_sampler.py` | uniform 与 loss-second-moment 时间步采样器 |
| `diffusion/diffusion_utils.py` | KL 与高斯对数似然工具 |

### 8.2 DiT³ 模型结构

以默认 `DiTTT-B/2`、ImageNet 256×256 为例：

```text
256×256 RGB
  → Stable Diffusion VAE encoder
  → 4×32×32 latent，乘 0.18215
  → 2×2 PatchEmbed
  → 16×16 = 256 tokens，每个 768 维
  → 加固定二维 sin-cos 位置编码
  → 12 个 DiT³ Block
  → adaLN 调制的 FinalLayer
  → unpatchify 成 8×32×32
     （4 通道噪声预测 + 4 通道可学习方差）
```

时间步先做正弦频率编码再经两层 MLP；类别使用 Embedding。两者相加得到条件 `c=t+y`。训练时类别以 0.1 概率替换为空类别，为 classifier-free guidance（CFG）提供无条件分支。

每个 DiT³ Block：

```text
CPE
→ 无仿射 LayerNorm
→ 条件 shift/scale
→ TTT
→ 条件 gate + residual
→ 无仿射 LayerNorm
→ 条件 shift/scale
→ MLP
→ 条件 gate + residual
```

adaLN 的调制输出层和最终输出层均零初始化，即 adaLN-Zero。与原始 DiT 相比，本项目把自注意力替换成了 TTT，并增加 CPE。

支持的模型规格：

| 系列 | 深度 | 宽度 | 头数 | Patch 选项 |
|---|---:|---:|---:|---|
| S | 12 | 384 | 6 | 2 / 4 / 8 |
| B | 12 | 768 | 12 | 2 / 4 / 8 |
| L | 24 | 1024 | 16 | 2 / 4 / 8 |
| XL | 28 | 1152 | 16 | 2 / 4 / 8 |

### 8.3 训练流程

`train.py` 的核心默认设置：

- 必须使用 CUDA + NCCL DDP；
- ImageNet 用 `torchvision.datasets.ImageFolder` 读取；
- 输入中心裁剪、随机水平翻转并归一化到 `[-1,1]`；
- VAE 将图像压缩 8 倍至 4 通道 latent；
- 扩散过程为 1000 步 linear beta schedule；
- 默认预测噪声并学习方差，训练损失为 MSE 体系；
- AdamW，固定学习率 `1e-4`，`weight_decay=0`；
- 默认 1400 epoch、全局 batch size 256；
- EMA decay 为 `0.9999`；
- 每 100 step 记录日志，每 50,000 step 保存模型、EMA、优化器和参数；
- 默认开启 TF32，以提高 Ampere GPU 上的速度。

```bash
cd dittt
torchrun --nnodes=1 --nproc_per_node=<GPU_NUM> \
  train.py --model DiTTT-B/2 \
  --data-path /path/to/imagenet/train \
  --image-size 256 --global-batch-size 256
```

### 8.4 采样与评估

单进程采样：

```bash
python sample.py --model DiTTT-B/2 --image-size 256 \
  --ckpt /path/to/model.pt --cfg-scale 4.0 \
  --num-sampling-steps 250 --seed 0
```

它会：

1. 从高斯噪声开始；
2. 将有条件与无条件 batch 拼接；
3. 通过 `forward_with_cfg()` 做 CFG；
4. 使用 respaced DDPM `p_sample_loop` 从 1000 步中选 250 步；
5. 用 VAE 解码并保存 `sample.png`。

批量评估：

```bash
torchrun --nnodes=1 --nproc_per_node=<GPU_NUM> \
  sample_ddp.py --model DiTTT-B/2 --image-size 256 \
  --ckpt /path/to/model.pt --num-fid-samples 50000 \
  --sample-dir samples --cfg-scale 1 \
  --num-sampling-steps 250
```

每个 rank 保存一部分 PNG，rank 0 最后将前 50,000 张打包为 `.npz`，供 guided-diffusion 的 TensorFlow evaluator 计算 FID、IS、sFID、Precision 和 Recall。

为复现原 DiT 行为，`forward_with_cfg()` 默认只对输出的前三个通道施加 guidance，而不是所有噪声通道。

## 9. 配置和权重如何对应

### 分类

- `vittt_{t,s,b}.yaml` → `main.py` → 单尺度 `vittt_tiny/small/base`；
- `h_vittt_{t,s,b}.yaml` → `main_ema.py` → 层次化模型，仅 EMA；
- `h_vittt_{t,s,b}_mesa.yaml` → `main_ema.py` → EMA + MESA。

### 检测/分割

下游权重文件名在配置中写死为：

```text
./data/H-ViTTT-T.pth
./data/H-ViTTT-S.pth
./data/H-ViTTT-B.pth
```

路径相对于合并后的 OpenMMLab 工程运行目录。下游 `init_weights()` 能识别三种 checkpoint 格式：

- 顶层直接是 state dict；
- 含 `model`；
- 含 `state_dict`。

加载使用 `strict=False`，分类头等不匹配参数会被忽略。

### DiT³

`download.find_model()` 在加载 `train.py` 生成的 checkpoint 时优先取 `checkpoint["ema"]`，因此采样默认使用 EMA 权重。

## 10. 阅读和复现时需要注意

1. **检测/分割目录不完整。** 必须合并到指定版本的完整 MMDetection/MMSegmentation，不能把当前目录直接当成独立工程。
2. **工作目录很重要。** `vittt/main.py` 和 `dittt/*.py` 使用顶层式本地导入，建议分别从 `vittt/`、`dittt/` 内运行。
3. **分类入口强制分布式。** 代码直接读取 `RANK/WORLD_SIZE/LOCAL_RANK` 并初始化 NCCL，普通 `python main.py` 会失败。
4. **旧版依赖 API。** 分类代码针对 PyTorch 1.11、timm 0.4.12 和旧式 `torch.cuda.amp`/`torch.distributed.launch`；升级环境时需要逐项验证。
5. **下游保留了兼容性导入。** 两个 backbone 文件仍有未使用的 `from ..builder import BACKBONES` 等旧式导入；若目标 OpenMMLab 安装中已移除对应模块，需按实际版本删除该未使用导入或改成 registry API。
6. **生成采样默认按 ImageNet-1K 编写。** `sample.py` 和 `sample_ddp.py` 将空类别 ID 硬编码为 `1000`；若修改 `--num-classes`，应同步改为空类别 ID `num_classes`。
7. **模型假设二维 token 网格。** 单尺度分类和 DiT³ 都用 `sqrt(N)` 恢复 `H/W`，因此正常路径要求正方形 token 网格。
8. **`temp.py` 是开发脚本。** 它不是正式测试套件，包含 FLOPs/CPU 同步等实验代码，不能替代训练或数值回归测试。
9. **checkpoint 配置快照扩展名有误导性。** 分类入口写入 `config.json`，实际内容来自 YACS `config.dump()`，格式更接近 YAML。
10. **许可证分层。** 根目录是 MIT License；`dittt/LICENSE.txt` 是 CC BY-NC 4.0。使用或再发布生成分支前应分别核对适用范围。

## 11. 推荐阅读顺序

如果目标是理解论文核心，而不是立即跑完整实验，建议按以下顺序：

1. `ttt_block.py`：先弄清两条内循环分支；
2. `vittt/models/h_vittt.py`：看 TTT 如何组成视觉 Block 和四阶段骨干；
3. `vittt/models/vittt.py`：对比单尺度纯 TTT；
4. `vittt/config.py`、`vittt/data/build.py`、`vittt/main_ema.py`：理解分类训练与 MESA；
5. `segmentation/mmseg/models/backbones/vittt.py`：理解动态尺寸和多尺度输出改造；
6. `dittt/models.py`：理解 TTT 如何替换 DiT attention；
7. `dittt/train.py` 与 `dittt/diffusion/`：最后串起 VAE、扩散损失和采样。

最小的代码级验证可以从根目录示例开始：

```python
import torch
from ttt_block import TTT

block = TTT(dim=512, num_heads=16)
x = torch.rand(1, 256, 512)
y = block(x, h=16, w=16)
print(y.shape)  # torch.Size([1, 256, 512])
```
