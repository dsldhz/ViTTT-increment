# ViT³ / DiT³ 模型推理指南

> 本文只关注已经训练好的模型如何推理，不讨论训练流程。内容根据当前仓库代码、仓库 README，以及指定版本的 OpenMMLab 官方说明整理。

## 1. 先看结论

| 任务 | 模型 | 推理输入 | 是否需要下载数据集 | 主要输出 |
|---|---|---|---|---|
| 图像分类 | ViT³-T/S/B、H-ViT³-T/S/B | ImageNet 格式图片目录 | 使用仓库现有评测入口时需要 | Top-1/Top-5 准确率 |
| 目标检测/实例分割 | H-ViT³ + Mask R-CNN | 任意 JPG/PNG，或 COCO val2017 | 单图不需要；评测需要 | bbox、类别、置信度、mask |
| 语义分割 | H-ViT³ + UperNet | 任意 JPG/PNG，或 ADE20K validation | 单图不需要；评测需要 | 150 类像素分割图 |
| 图像生成 | DiT³-B/2、B/4、B/8 | 随机噪声 + ImageNet 类别 ID | 不需要输入图片 | 256×256 生成图片 |

推荐分别使用三个环境：

1. `vittt-infer`：分类模型；
2. `vittt-openmmlab`：检测和分割；
3. `DiTTT`：扩散图像生成。

GPU 推理最省事。分类仓库的现有评测入口强制使用 NCCL，因此必须使用 NVIDIA GPU；检测、分割和 `dittt/sample.py` 理论上支持 CPU，但速度会明显变慢。

## 2. 分类模型推理

### 2.1 环境

仓库声明的版本：

- Python 3.9
- PyTorch 1.11.0
- torchvision 0.12.0
- timm 0.4.12
- numpy、einops、yacs

实际运行还需要 `PyYAML`、`termcolor` 和 `Pillow`。

```bash
conda create -n vittt-infer python=3.9 -y
conda activate vittt-infer

# CUDA 11.3 示例；如果机器 CUDA/驱动不匹配，请换成对应的 PyTorch 构建
conda install pytorch==1.11.0 torchvision==0.12.0 \
  cudatoolkit=11.3 -c pytorch -y

pip install timm==0.4.12 numpy einops yacs PyYAML termcolor Pillow
```

不要随意升级 `timm`，代码使用的是旧版模块路径和注册 API。

### 2.2 分类权重

以下链接均来自仓库 `vittt/README.md`。

| 模型 | 配置 | 官方权重 |
|---|---|---|
| ViT³-T | `vittt/cfgs/vittt_t.yaml` | [TsinghuaCloud 下载](https://cloud.tsinghua.edu.cn/f/e12d1bd1c20c4e06a2da/?dl=1) |
| ViT³-S | `vittt/cfgs/vittt_s.yaml` | [TsinghuaCloud 下载](https://cloud.tsinghua.edu.cn/f/4ab59ca54b474dff9b70/?dl=1) |
| ViT³-B | `vittt/cfgs/vittt_b.yaml` | [TsinghuaCloud 下载](https://cloud.tsinghua.edu.cn/f/c9e9bb09f85d449ab8cf/?dl=1) |
| H-ViT³-T | `vittt/cfgs/h_vittt_t_mesa.yaml` | [TsinghuaCloud 下载](https://cloud.tsinghua.edu.cn/f/f0c7c73ceabe49b7b161/?dl=1) |
| H-ViT³-S | `vittt/cfgs/h_vittt_s_mesa.yaml` | [TsinghuaCloud 下载](https://cloud.tsinghua.edu.cn/f/3eeab8621a7f43929a87/?dl=1) |
| H-ViT³-B | `vittt/cfgs/h_vittt_b_mesa.yaml` | [TsinghuaCloud 下载](https://cloud.tsinghua.edu.cn/f/1d0d7fb840254275b29d/?dl=1) |

建议保存为：

```text
ViTTT-increment/
└── weights/
    ├── ViTTT-T.pth
    ├── ViTTT-S.pth
    ├── ViTTT-B.pth
    ├── H-ViTTT-T.pth
    ├── H-ViTTT-S.pth
    └── H-ViTTT-B.pth
```

文件名可以自定义，推理时通过 `--resume` 传入实际路径。

### 2.3 分类输入从哪里获取

仓库现成的推理入口是 ImageNet-1K 验证集评测，而不是单张图片预测。数据可从：

- [ImageNet 官方下载页面](https://www.image-net.org/download.php)
- [ILSVRC 2012 官方页面](https://ftp.image-net.org/challenges/LSVRC/2012/index.php)

获得。ImageNet 通常需要注册并同意数据使用条款。

目录必须整理成：

```text
/data/imagenet/
├── train/
│   ├── n01440764/
│   │   └── *.JPEG
│   └── ...共 1000 个类别目录
└── val/
    ├── n01440764/
    │   └── *.JPEG
    └── ...共 1000 个类别目录
```

需要特别注意：当前 `main.py`/`main_ema.py` 即使处于 `--eval` 模式，也会先同时构建 `train/` 和 `val/` 数据集。因此使用现有命令时，只有 `val/` 而没有 `train/` 会报错。

### 2.4 ViT³ 推理命令

从 `vittt/` 目录运行。下面用单 GPU 举例：

```bash
cd /path/to/ViTTT-increment/vittt

torchrun --standalone --nproc_per_node=1 \
  main.py \
  --cfg cfgs/vittt_s.yaml \
  --data-path /data/imagenet \
  --eval \
  --resume ../weights/ViTTT-S.pth \
  --output ./output_eval
```

输出日志中会给出：

```text
Acc@1 ...
Acc@5 ...
```

多 GPU 只需调整进程数：

```bash
torchrun --standalone --nproc_per_node=8 \
  main.py \
  --cfg cfgs/vittt_b.yaml \
  --data-path /data/imagenet \
  --eval \
  --resume ../weights/ViTTT-B.pth \
  --output ./output_eval
```

### 2.5 H-ViT³ 推理命令

H-ViT³ 使用 `main_ema.py`：

```bash
cd /path/to/ViTTT-increment/vittt

torchrun --standalone --nproc_per_node=1 \
  main_ema.py \
  --cfg cfgs/h_vittt_s_mesa.yaml \
  --data-path /data/imagenet \
  --eval \
  --resume ../weights/H-ViTTT-S.pth \
  --output ./output_eval
```

该入口会分别评估普通模型权重和 checkpoint 中的 EMA 权重。

### 2.6 如果只想输入一张自己的图片

当前仓库没有提供分类单图 CLI。直接使用 `main.py --eval` 仍然需要完整的 ImageNet 目录。

单图推理需要自行写一个很短的入口，主要步骤是：

1. 用对应 YAML 调用 `get_config()` 和 `build_model()`；
2. 从 checkpoint 的 `model` 字段加载权重；
3. 图片按 `Resize(256) → CenterCrop(224) → ToTensor → ImageNet Normalize` 处理；
4. 模型输出为 `[1,1000]` logits，取 `argmax`；
5. 类别 ID 可使用 [PyTorch Hub 的 1000 类名称表](https://raw.githubusercontent.com/pytorch/hub/master/imagenet_classes.txt) 转成人类可读名称。

分类输入可以是任意 JPG/PNG，但模型只会从 ImageNet-1K 的 1000 个类别中选择结果。

## 3. 检测和分割的共享环境

检测和分割目录只是增量文件，必须分别合并到完整的官方工程：

- MMDetection 3.3.0；
- MMSegmentation 1.2.2。

### 3.1 建议环境

```bash
conda create -n vittt-openmmlab python=3.9 -y
conda activate vittt-openmmlab

# 先按显卡驱动安装匹配的 PyTorch 和 torchvision
# 下面仅为 CUDA 11.7 示例
conda install pytorch torchvision pytorch-cuda=11.7 \
  -c pytorch -c nvidia -y

pip install -U openmim
mim install "mmengine>=0.7.4,<1.0.0"
mim install "mmcv>=2.0.0,<2.2.0"
pip install timm
```

版本范围说明：

- MMDetection 3.3.0 要求 `mmcv>=2.0.0,<2.2.0`、`mmengine>=0.7.1,<1.0.0`；
- MMSegmentation 1.2.2 要求 `mmcv>=2.0.0`、`mmengine>=0.7.4`；
- 上述交集可以让两者共享同一个基础环境。

### 3.2 安装完整工程并合并本仓库代码

检测：

```bash
git clone --branch v3.3.0 \
  https://github.com/open-mmlab/mmdetection.git
cd mmdetection
pip install -v -e .
```

然后将本仓库 `detection/` 里的 `configs/`、`mmdet/` 增量文件合并到这个完整工程的同名目录。

分割：

```bash
git clone --branch v1.2.2 \
  https://github.com/open-mmlab/mmsegmentation.git
cd mmsegmentation
pip install -v -e .
```

然后将本仓库 `segmentation/` 里的 `configs/`、`mmseg/` 增量文件合并到完整工程。

### 3.3 MMDetection 3.3.0 的必要兼容处理

当前增量文件：

```text
detection/mmdet/models/backbones/vittt.py
```

包含下面这行旧式、且未使用的导入：

```python
from ..builder import BACKBONES
```

MMDetection 3.3.0 已没有 `mmdet/models/builder.py`。合并后若出现：

```text
ModuleNotFoundError: No module named 'mmdet.models.builder'
```

删除上面这一行即可；骨干实际通过 `mmdet.registry.MODELS` 注册，不依赖 `BACKBONES`。

MMSegmentation 1.2.2 仍保留兼容用的 `mmseg/models/builder.py`，不需要做这个修改。

## 4. 目标检测与实例分割推理

### 4.1 最终检测权重

这些是完整的 Mask R-CNN 任务权重，不是只有 backbone。

| 模型 | 训练周期 | 配置 | 官方权重 |
|---|---:|---|---|
| H-ViT³-T Mask R-CNN | 1x | `vittt_t_mrcnn_1x.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/a00315bbfd504edebfb0/?dl=1) |
| H-ViT³-S Mask R-CNN | 1x | `vittt_s_mrcnn_1x.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/b42d36b21a5e44d0b266/?dl=1) |
| H-ViT³-B Mask R-CNN | 1x | `vittt_b_mrcnn_1x.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/4a641d087ca248e8804b/?dl=1) |
| H-ViT³-T Mask R-CNN | 3x | `vittt_t_mrcnn_3x.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/8c39ef3d7d144a81b986/?dl=1) |
| H-ViT³-S Mask R-CNN | 3x | `vittt_s_mrcnn_3x.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/faa577d9a25140ef94d1/?dl=1) |
| H-ViT³-B Mask R-CNN | 3x | `vittt_b_mrcnn_3x.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/e9c1aef780d949489408/?dl=1) |

通常优先选择精度更高的 3x 权重。

### 4.2 还需要对应的 ImageNet backbone 权重

六个检测配置的 `model.backbone.init_cfg` 都写死了：

```text
./data/H-ViTTT-T.pth
./data/H-ViTTT-S.pth
./data/H-ViTTT-B.pth
```

即使随后会加载完整 Mask R-CNN checkpoint，MMEngine 初始化模型时仍可能先访问这个路径。为避免初始化时报文件不存在，按模型大小再下载对应分类权重：

| Backbone | 权重 |
|---|---|
| H-ViT³-T | [下载](https://cloud.tsinghua.edu.cn/f/f0c7c73ceabe49b7b161/?dl=1) |
| H-ViT³-S | [下载](https://cloud.tsinghua.edu.cn/f/3eeab8621a7f43929a87/?dl=1) |
| H-ViT³-B | [下载](https://cloud.tsinghua.edu.cn/f/1d0d7fb840254275b29d/?dl=1) |

在 MMDetection 根目录放置为：

```text
mmdetection/
├── data/
│   ├── H-ViTTT-T.pth
│   ├── H-ViTTT-S.pth
│   └── H-ViTTT-B.pth
└── checkpoints/
    └── vittt_s_mrcnn_3x.pth
```

只运行 Small 时只需下载 `H-ViTTT-S.pth`。

### 4.3 检测输入从哪里获取

单图推理可以使用任意 JPG/PNG，不需要 COCO 数据集。输出类别仍限制在 COCO 的 80 类。

要复现 README 中的 bbox/mask mAP，需要从 [COCO 官方网站](https://cocodataset.org/#download) 下载：

- 2017 Validation images；
- 2017 Train/Val annotations。

配置期望：

```text
/data/coco/
├── val2017/
│   └── *.jpg
└── annotations/
    └── instances_val2017.json
```

并将 `configs/_base_/datasets/coco_instance.py` 中的 `data_root` 修改为实际路径。

### 4.4 任意单图推理命令

在合并后的 MMDetection 3.3.0 根目录：

```bash
python demo/image_demo.py \
  /path/to/input.jpg \
  configs/vittt/vittt_s_mrcnn_3x.py \
  --weights checkpoints/vittt_s_mrcnn_3x.pth \
  --device cuda:0 \
  --pred-score-thr 0.3 \
  --out-dir outputs/vittt_s
```

结果通常保存在：

```text
outputs/vittt_s/
├── vis/           # bbox 和 mask 可视化
└── preds/         # 结构化预测
```

也可以把输入参数换成图片目录，MMDetection 3.3.0 的 `image_demo.py` 支持图片文件或目录。

### 4.5 COCO 验证集评测

```bash
python tools/test.py \
  configs/vittt/vittt_s_mrcnn_3x.py \
  checkpoints/vittt_s_mrcnn_3x.pth
```

多 GPU：

```bash
tools/dist_test.sh \
  configs/vittt/vittt_s_mrcnn_3x.py \
  checkpoints/vittt_s_mrcnn_3x.pth \
  8
```

输出为 COCO bbox mAP 和 mask mAP。

## 5. 语义分割推理

### 5.1 最终分割权重

这些是完整的 UperNet 分割模型权重。

| 模型 | 配置 | 官方权重 |
|---|---|---|
| H-ViT³-T + UperNet | `vittt_tiny.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/df39e508ae5b49c59cca/?dl=1) |
| H-ViT³-S + UperNet | `vittt_small.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/71c786dd7e8046b5ab43/?dl=1) |
| H-ViT³-B + UperNet | `vittt_base.py` | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/6bd6db157003462f9fed/?dl=1) |

建议目录：

```text
mmsegmentation/
├── data/
│   ├── H-ViTTT-T.pth
│   ├── H-ViTTT-S.pth
│   └── H-ViTTT-B.pth
└── checkpoints/
    └── vittt_small_upernet_160k.pth
```

和检测相同，配置中仍包含 ImageNet backbone 初始化路径，所以还需从分类权重表下载对应的 `H-ViTTT-T/S/B.pth` 放入 `data/`。

### 5.2 分割输入从哪里获取

单图推理可以使用任意 JPG/PNG，不需要下载 ADE20K。模型输出语义限制在 ADE20K 的 150 类。

要复现 mIoU，需要从以下官方入口获得 ADE20K：

- [ADE20K 官方页面](https://ade20k.csail.mit.edu/)
- [MIT Scene Parsing/ADE20K 页面](https://groups.csail.mit.edu/vision/ADE20K/index.html)

配置期望的关键目录是：

```text
/data/ade20k/
├── images/
│   ├── training/
│   └── validation/
└── annotations/
    ├── training/
    └── validation/
```

修改 `configs/_base_/datasets/ade20k.py` 中的 `data_root`。

### 5.3 任意单图推理命令

在合并后的 MMSegmentation 1.2.2 根目录：

```bash
python demo/image_demo.py \
  /path/to/input.jpg \
  configs/vittt/vittt_small.py \
  checkpoints/vittt_small_upernet_160k.pth \
  --device cuda:0 \
  --opacity 0.5 \
  --out-file outputs/vittt_small_seg.png
```

如果输出目录不存在，先创建：

```bash
mkdir -p outputs
```

最终文件是原图与语义分割 mask 的叠加可视化。

### 5.4 ADE20K 验证集评测

```bash
python tools/test.py \
  configs/vittt/vittt_small.py \
  checkpoints/vittt_small_upernet_160k.pth
```

多 GPU：

```bash
tools/dist_test.sh \
  configs/vittt/vittt_small.py \
  checkpoints/vittt_small_upernet_160k.pth \
  8
```

输出主要指标为 mIoU。

## 6. DiT³ 图像生成推理

### 6.1 环境

生成目录已经提供环境文件：

```bash
cd /path/to/ViTTT-increment/dittt
conda env create -f environment.yml
conda activate DiTTT
```

其中主要依赖是：

- Python >= 3.8
- PyTorch >= 1.13
- torchvision
- CUDA 11.7
- timm
- diffusers
- accelerate
- tqdm

首次采样时还会自动下载 Stable Diffusion VAE。代码使用：

- `stabilityai/sd-vae-ft-mse`
- `stabilityai/sd-vae-ft-ema`

可从 Hugging Face 查看或提前缓存：

- [sd-vae-ft-mse](https://huggingface.co/stabilityai/sd-vae-ft-mse)
- [sd-vae-ft-ema](https://huggingface.co/stabilityai/sd-vae-ft-ema)

### 6.2 DiT³ 权重

| 模型 | 输入 latent patch | 官方权重 |
|---|---:|---|
| DiT³-B/2 | 2×2 | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/c68d33055c6b44f39584/?dl=1) |
| DiT³-B/4 | 4×4 | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/49a2ea7a60624a8a94f3/?dl=1) |
| DiT³-B/8 | 8×8 | [TsinghuaCloud](https://cloud.tsinghua.edu.cn/f/d09682e5b50d40d48f4e/?dl=1) |

保存示例：

```text
ViTTT-increment/
└── weights/
    ├── DiTTT-B-2.pt
    ├── DiTTT-B-4.pt
    └── DiTTT-B-8.pt
```

模型名称必须和权重匹配，例如 B/4 权重必须传 `--model DiTTT-B/4`。

### 6.3 生成模型的输入从哪里获取

不需要下载输入图片或 ImageNet 数据集。

`sample.py` 自己生成高斯随机噪声，并使用代码中的 ImageNet 类别 ID：

```python
class_labels = [207, 360, 387, 974, 88, 979, 417, 279]
```

类别 ID 范围是 `0～999`。要生成其他物体，需要修改 `dittt/sample.py` 中的 `class_labels`。类别 ID 与名称可以查：

- [PyTorch Hub ImageNet 1000 类名称表](https://raw.githubusercontent.com/pytorch/hub/master/imagenet_classes.txt)

例如该文件第 208 行附近对应 golden retriever；Python 列表从 0 开始，因此需要按 0-based index 使用。

`--seed` 控制随机噪声；类别和 seed 相同通常会得到可复现的输出。

### 6.4 单进程生成命令

```bash
cd /path/to/ViTTT-increment/dittt

python sample.py \
  --model DiTTT-B/2 \
  --image-size 256 \
  --ckpt ../weights/DiTTT-B-2.pt \
  --vae mse \
  --cfg-scale 4.0 \
  --num-sampling-steps 250 \
  --seed 0
```

结果保存到：

```text
dittt/sample.png
```

脚本一次生成 8 个预设类别，并排成图片网格。

参数含义：

| 参数 | 含义 |
|---|---|
| `--model` | 必须与 checkpoint 结构一致 |
| `--image-size` | 当前官方权重表对应 256 |
| `--vae` | `mse` 或 `ema`，单图脚本默认 `mse` |
| `--cfg-scale` | classifier-free guidance 强度 |
| `--num-sampling-steps` | 去噪步数，默认/示例为 250 |
| `--seed` | 随机种子 |

### 6.5 多 GPU 批量生成

生成 50,000 张图片用于 FID：

```bash
cd /path/to/ViTTT-increment/dittt

torchrun --standalone --nproc_per_node=8 \
  sample_ddp.py \
  --model DiTTT-B/2 \
  --image-size 256 \
  --ckpt ../weights/DiTTT-B-2.pt \
  --num-fid-samples 50000 \
  --sample-dir samples \
  --cfg-scale 1 \
  --num-sampling-steps 250
```

输出包括：

- 每张生成图片的 PNG；
- 包含 50,000 张图片的 `.npz`。

`sample_ddp.py` 的类别不是从外部输入，而是在每个 batch 中从 `0～999` 随机采样。

## 7. 最小下载清单

### 只做分类

- 一个 ViT³/H-ViT³ 分类 checkpoint；
- 完整 ImageNet `train/` 和 `val/`，因为现有评测入口会同时构建两者。

### 只对自己的图片做检测

- 完整 MMDetection 3.3.0 环境；
- 一个最终 Mask R-CNN checkpoint；
- 对应的 H-ViT³ ImageNet backbone checkpoint；
- 自己的一张 JPG/PNG；
- 不需要 COCO 数据集。

### 只对自己的图片做分割

- 完整 MMSegmentation 1.2.2 环境；
- 一个最终 UperNet checkpoint；
- 对应的 H-ViT³ ImageNet backbone checkpoint；
- 自己的一张 JPG/PNG；
- 不需要 ADE20K 数据集。

### 只做图像生成

- `dittt/environment.yml` 环境；
- 一个 DiT³ checkpoint；
- Hugging Face VAE，首次运行自动获取；
- 不需要任何输入图片或数据集。

## 8. 常见错误

### `KeyError: RANK` 或 `RANK is not set`

分类入口不能直接执行 `python main.py`，要使用：

```bash
torchrun --standalone --nproc_per_node=1 main.py ...
```

### `No module named mmdet.models.builder`

删除合并后 `mmdet/models/backbones/vittt.py` 中未使用的：

```python
from ..builder import BACKBONES
```

### 找不到 `./data/H-ViTTT-S.pth`

检测/分割配置会先加载 ImageNet backbone。下载相同大小的 H-ViT³ 分类权重，并按配置中的文件名放入 OpenMMLab 工程 `data/`。

### 加载 checkpoint 时大量 missing/unexpected keys

先检查模型与权重是否严格对应：

- Tiny / Small / Base；
- 检测 1x / 3x 的配置与任务 checkpoint；
- DiT³ B/2 / B/4 / B/8。

### DiT³ 首次运行卡在 VAE 下载

首次执行需要访问 Hugging Face。可提前下载/缓存 `stabilityai/sd-vae-ft-mse`，或设置 Hugging Face 镜像和缓存目录。

### 自定义 `--num-classes` 后 DiT³ CFG 报错

`sample.py` 和 `sample_ddp.py` 把空类别 ID 写死为 `1000`。当前官方权重就是 ImageNet-1K，保持 `--num-classes 1000` 最稳妥。

## 9. 参考入口

- [项目分类 README](vittt/README.md)
- [项目检测 README](detection/README.md)
- [项目分割 README](segmentation/README.md)
- [项目生成 README](dittt/README.md)
- [MMDetection 3.3.0 安装说明](https://github.com/open-mmlab/mmdetection/blob/v3.3.0/docs/en/get_started.md)
- [MMSegmentation 1.2.2 安装说明](https://github.com/open-mmlab/mmsegmentation/blob/v1.2.2/docs/en/get_started.md)
- [COCO 官方网站](https://cocodataset.org/)
- [ADE20K 官方网站](https://ade20k.csail.mit.edu/)
- [ImageNet 官方下载](https://www.image-net.org/download.php)
